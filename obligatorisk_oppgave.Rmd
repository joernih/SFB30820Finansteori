---
title: "obligatorisk oppgave"
output: html_document
---

## Tema 1: Forventet kontantsrøm

## Tema 2: Kapitalkostand

## Tema 3: Gjeld vs. egenkapital

## Tema 4: Finansielle instrumenter
Gi en presis forklaring på begrepet finansielle instrumenter

## Excel-del
Bitcoin-find data

rm(list = ls())  # reset global variables
#import the libraries we need
library(jsonlite)
library(glue)

# create a function to retrieve daily data
retreive_daily_data <- function(pair, filename) {
    url = glue("https://api.pro.coinbase.com/products/{pair}/candles?granularity=86400")
    columnNames <- c('unix', 'low', 'high', 'open', 'close', glue('{pair} volume'))
    mydata <- fromJSON(url)
    df <- as.data.frame(mydata)
    colnames(df) <- columnNames  # rename the columns

    write.csv(df, file = filename)
    }


newPair <- "BTC-USD"
fileName <- glue("dailyData{newPair}.csv")
runFunc <- retreive_daily_data(newPair, filename = fileName)
runFunc
https://www.cryptodatadownload.com/blog/how-to-download-coinbase-price-data-R.html

https://www.codingfinance.com/post/2018-04-05-portfolio-returns/
